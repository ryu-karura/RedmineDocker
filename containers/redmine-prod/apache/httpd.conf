# ServerRoot
ServerRoot "/etc/httpd"

# Load modules required for Redmine reverse proxy
LoadModule mpm_event_module modules/mod_mpm_event.so
LoadModule log_config_module modules/mod_log_config.so
LoadModule mime_module modules/mod_mime.so
LoadModule alias_module modules/mod_alias.so
LoadModule dir_module modules/mod_dir.so
LoadModule authz_core_module modules/mod_authz_core.so
LoadModule authz_host_module modules/mod_authz_host.so
LoadModule env_module modules/mod_env.so
LoadModule headers_module modules/mod_headers.so
LoadModule rewrite_module modules/mod_rewrite.so
LoadModule proxy_module modules/mod_proxy.so
LoadModule proxy_http_module modules/mod_proxy_http.so
LoadModule unixd_module modules/mod_unixd.so
LoadModule setenvif_module modules/mod_setenvif.so

# Run httpd as root (container-internal; the Puma process runs as redmine_adm)
User root
Group root

ServerAdmin root@localhost
ServerName localhost

# Disable server version disclosure in error pages and headers
ServerTokens Prod
ServerSignature Off

# Default MIME types
TypesConfig /etc/mime.types

# Log format
LogFormat "%h %l %u %t \"%r\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined
LogFormat "%h %l %u %t \"%r\" %>s %b" common

# Logs go to stdout/stderr for systemd journal capture
ErrorLog  /proc/self/fd/2
CustomLog /proc/self/fd/1 combined

LogLevel warn

# Global proxy settings
ProxyRequests Off
ProxyPreserveHost On

# Include virtual host configuration
IncludeOptional conf.d/*.conf
